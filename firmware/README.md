# firmware/ (Part B — not started)

STM32CubeIDE project for the Nucleo-F303RE slave host will live here:
SOES stack over SPI to the AX58100, PWM out to the SG90.

Starts when the hardware arrives — follow `docs/bringup-checklist.md`
Phase 4. The process-data layout it must implement is defined in
`master/src/pdo_layout.h`.
