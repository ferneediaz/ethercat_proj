build*/
*.o
*.out
test_angle
.DS_Store
.cache/

# Presentation write-up of the project, kept local rather than committed.
# It is a rendered artifact rather than source: a single ~220 KB HTML file
# that duplicates what docs/bringup-checklist.md and the code comments
# already record, and it would need updating on every hardware change.
docs/project-explainer.html
docs/video-script.html

# Handout: rendered presentation artifact, kept local like the others above.
docs/handout.html
docs/ethercat-servo-node.pdf

# ASIX vendor reference material for the AX58100: datasheets, app notes,
# schematics, Gerbers and TwinCAT demo sources. Third-party documents we did
# not write and are not ours to redistribute, and ~45 MB of binaries that
# would dwarf the actual source. Kept on the bench, not in the repo.
knowledge base/
